# 桌遊揪團站 — 建置指導書

> 給實作 AI 的完整規格。本文件是唯一事實來源，所有決策都已與需求方達成共識，**不要自行更動**。遇到本文件沒寫到的細節，先問需求方，不要自己發明。
>
> 介面語言：繁體中文。版面：手機優先。
> 版本：2026-09-12

---

## 0. 動工前需要需求方提供

實作前先向需求方索取下列項目，沒拿到就先用佔位值並在程式中標記 `TODO`：

| 項目 | 用途 | 佔位符 |
|---|---|---|
| Apps Script Web App 部署網址 | 前端讀寫資料 | `APPS_SCRIPT_URL` |
| 寫入用共用密碼 | 寫入驗證 | `WRITE_SECRET` |
| 成員名單（姓名） | `members` 分頁初始資料 | — |
| 地點清單（名稱＋持有者） | `venues` 分頁初始資料 | — |
| Google 試算表 | 資料庫 | 新建或沿用既有檔案 |
| `桌上遊戲相關` 資料夾存取權 | 產生 `games.json` | 含三個 xlsx |

---

## 1. 專案目標

給一群固定朋友用的小網站，解決三件事：

1. 什麼時間有誰可以約
2. 這個時間在哪裡可以玩
3. 想玩的桌遊在哪裡 / 誰手上

規模預期：成員 10–20 人、同時進行的團 0–5 個、遊戲總數約 200–300 款。**這是一個小工具，不是產品。**簡單、能用、好維護優先於任何架構上的講究。

---

## 2. 硬性技術約束

這些是已定案的限制，**不得變更**：

- **純靜態前端**：手寫 HTML ＋ 原生 JavaScript。**不使用任何前端框架**（React / Vue / Svelte / Alpine 都不行），**不使用建置流程**，專案裡**不得有 `package.json` 或 `node_modules`**（唯一例外見 §7 的抓取腳本，它只在本機跑，不進部署）。
- **部署**：GitHub repo → Vercel。Vercel 設定為靜態託管，Build Command 留空，Output Directory 為專案根目錄。
- **資料庫**：Google Sheets，透過 Google Apps Script Web App 讀寫。**不接任何其他資料庫服務。**
- **無登入系統**：不做 OAuth、不做 session、不做使用者表。
- **執行時不呼叫 BGG**：BGG 資料一律事先抓好存成靜態 JSON。
- 前端 JS 以原生 ES modules 撰寫（`<script type="module">`），瀏覽器直接載入，不打包。

---

## 3. 核心資料模型

### 3.1 持有者（holder）

每一筆「某人擁有某款遊戲」的紀錄都掛在一個**持有者**底下。持有者有兩種：

- `person` — 一個人（例：Sufu、阿明）
- `group` — 一個團體（例：桌遊社團的公有遊戲）

`group` 型持有者**不能投票、不能報名**，在「點自己的名字」的選單裡不出現。

### 3.2 場地（venue）

每個場地都指向一個持有者。例：

- 「Sufu 家」→ 持有者 `sufu`
- 「社團教室」→ 持有者 `club`

### 3.3 可玩清單規則（本專案最核心的規則）

> **一個團的可玩清單 ＝（所有已報名玩家各自的收藏）∪（該場地持有者的收藏）**

實作細節：

```
holderIds = new Set()
if (event.venue_id) holderIds.add(venueOf(event.venue_id).holder_id)
for (const s of signupsOf(event.id)) holderIds.add(memberOf(s.member_id).id)

games = allOwnerships.filter(o => holderIds.has(o.holder_id))
```

- 以 `bgg_id` 去重，但**保留全部來源**：同一款可能同時是「場地現有」又是「阿明可帶來」。
- 每款標示來源：持有者等於場地持有者 → 標示 `場地現有`；否則標示 `{持有者名稱} 可帶來`。
- **只計算已報名的人**。未報名者的收藏不列入，否則清單會膨脹成全員聯集、篩選失去意義。
- 場地若使用自由輸入（無 `venue_id`），則清單只由已報名玩家的收藏構成。

---

## 4. 資料落點

資料分散在兩個地方，前端載入後以 `holder_id` / `bgg_id` 為鍵合併。

### 4.1 repo 內的靜態 JSON（由本機腳本產生後 push）

#### `data/games.json` — 遊戲後設資料目錄

以 `bgg_id` 為鍵的物件。這是**所有**遊戲的資料來源，不分持有者。

```json
{
  "224517": {
    "bgg_id": 224517,
    "name_zh": "磚塊時代",
    "name_en": "Brass: Birmingham",
    "bgg_url": "https://boardgamegeek.com/boardgame/224517/",
    "thumbnail": "https://cf.geekdo-images.com/....jpg",
    "min_players": 2,
    "max_players": 4,
    "playing_time": 120,
    "min_playtime": 60,
    "max_playtime": 120,
    "weight": 3.87,
    "year": 2018,
    "category": "S-EC",
    "retailer_url": "https://....",
    "retail_price": 1680,
    "retailer": "XXX",
    "is_expansion": false,
    "parent_bgg_id": null
  }
}
```

- `category` 使用需求方既有的 20 碼分類系統（見 §4.3）。
- `retailer_url` / `retail_price` / `retailer` 來自 `桌遊資料庫.xlsx`，可能為 `null`。
- 擴充品項 `is_expansion: true`，並以 `parent_bgg_id` 指向母款。

#### `data/collections.json` — Sufu 的收藏歸屬

```json
[
  { "holder_id": "sufu", "bgg_id": 224517 },
  { "holder_id": "sufu", "bgg_id": 167791 }
]
```

### 4.2 Google Sheets（可即時編輯，不用 push）

同一張試算表，六個分頁。**第一列為欄位名，必須與下列完全一致。**

#### 分頁 `members`

| id | name | type | active |
|---|---|---|---|
| sufu | Sufu | person | TRUE |
| ming | 阿明 | person | TRUE |
| club | 桌遊社 | group | TRUE |

- `type` 為 `person` 或 `group`。
- `active` 為 `FALSE` 者不出現在任何選單，但既有資料仍正常顯示。

#### 分頁 `venues`

| id | name | holder_id | note | active |
|---|---|---|---|---|
| sufu_home | Sufu 家 | sufu | 有大桌 | TRUE |
| club_room | 社團教室 | club | | TRUE |

#### 分頁 `collections` — Sufu 以外的持有者的收藏

| holder_id | bgg_id | name_zh | note |
|---|---|---|---|
| ming | 266192 | 翼展 | |
| club | 174430 | 淞海城 | 社辦公有 |

- 由朋友自行維護，試算表編輯權開給他們。
- `name_zh` 是給填表的人辨識用的備援名稱。若 `bgg_id` 在 `games.json` 裡找不到，前端仍要顯示這款（用 `name_zh` ＋ BGG 連結），但標示「缺少遊戲資料」，且**排除在人數／時長／複雜度篩選之外**。絕對不要因為查無資料就整筆略過。

#### 分頁 `events`

| event_id | created_at | creator_id | title | status | venue_id | venue_free_text | confirmed_slot_id | game_bgg_ids | note |
|---|---|---|---|---|---|---|---|---|---|

- `status`：`open`（投票中）／`confirmed`（已定案）／`cancelled`（取消）。「已結束」不是欄位值，由日期推算（見 §6.5）。
- `venue_id` 與 `venue_free_text` 二擇一，可皆為空（尚未決定地點）。
- `game_bgg_ids`：逗號分隔，**選填**。

#### 分頁 `slots`

| slot_id | event_id | date | period | label |
|---|---|---|---|---|

- `date` 格式 `YYYY-MM-DD`。
- `period`：`afternoon` 或 `evening`。
- `label`：顯示用文字，例「10/4（六）下午」。由後端在建立時產生。

#### 分頁 `votes`

| event_id | slot_id | member_id | ok | updated_at |
|---|---|---|---|---|

- `ok` 為 `TRUE` / `FALSE`。**只有兩態，不要做第三態。**
- 同一 `(event_id, slot_id, member_id)` 只能有一列，重複投票為更新而非新增。

#### 分頁 `signups`

| event_id | member_id | joined_at |
|---|---|---|

### 4.3 分類代碼（供篩選用）

```
策略 S：S-WP 工擺、S-DC 牌組引擎建構、S-TR 旅行區域控制、
        S-4X 4X或戰鬥、S-EC 經貿競標、S-AC 行動選擇
派對 P：P-CS 陣營心機、P-C 卡牌、P-H 技巧、P-CR 創意
家庭 F：F-T 雙人、F-C 卡牌、F-S 輕策略、F-TL 板塊拼放、
        F-A 抽象、F-D 骰子
兒童 C：C-R 反應、C-H 巧手、C-M 記憶、C-S 兒童策略
```

---

## 5. Apps Script API 契約

### 5.1 部署設定

- 部署類型：Web app
- Execute as：Me
- Who has access：**Anyone**（必須，否則前端無法呼叫）
- 寫入密碼存在 Script Properties 的 `WRITE_SECRET`，**不要寫死在 `Code.gs` 裡**。

### 5.2 CORS 處理（重要，這是已知的坑）

- **讀取用 GET**，直接 `fetch(url + '?action=...')`。
- **寫入用 POST，且 `Content-Type` 必須是 `text/plain;charset=utf-8`**，body 放 JSON 字串。用 `application/json` 會觸發 preflight，而 Apps Script 無法回應 `OPTIONS`，請求會直接失敗。

```js
await fetch(APPS_SCRIPT_URL, {
  method: 'POST',
  headers: { 'Content-Type': 'text/plain;charset=utf-8' },
  body: JSON.stringify({ action: 'vote', secret, ...payload })
})
```

- Apps Script 端一律用 `ContentService.createTextOutput(JSON.stringify(res)).setMimeType(ContentService.MimeType.JSON)` 回應。
- **備援方案**：若實測發現瀏覽器仍因缺少 `Access-Control-Allow-Origin` 而讀不到回應內容，改為「送出後不讀回應（`mode: 'no-cors'`），接著重新 GET 一次狀態來確認寫入成功」。請在實作時實測並在 README 註明採用哪一種。

### 5.3 端點

#### `GET ?action=bootstrap`

一次回傳所有 Sheets 資料，前端啟動時呼叫一次。

```json
{
  "ok": true,
  "members": [...],
  "venues": [...],
  "collections": [...],
  "events": [...],
  "slots": [...],
  "votes": [...],
  "signups": [...]
}
```

#### `POST` — 所有寫入動作

共同欄位：`action`、`secret`。`secret` 不符一律回 `{ "ok": false, "error": "BAD_SECRET" }`，HTTP 狀態仍為 200。

| action | payload | 行為 |
|---|---|---|
| `createEvent` | `creator_id, title, slots[{date, period}], venue_id?, venue_free_text?, game_bgg_ids?, note?` | 建立團與其時段，`status` 設為 `open`。回傳新的 `event_id` |
| `vote` | `event_id, member_id, votes[{slot_id, ok}]` | 覆寫該成員在該團的所有投票 |
| `confirm` | `event_id, slot_id, venue_id?, venue_free_text?` | 設 `confirmed_slot_id`、`status='confirmed'`，並把**對該時段投 TRUE 的人**全部寫入 `signups`（已存在者略過） |
| `toggleSignup` | `event_id, member_id, join` | `join` 為 true 時新增報名，false 時移除 |
| `updateGames` | `event_id, game_bgg_ids` | 更新團的指定遊戲 |
| `cancelEvent` | `event_id` | `status='cancelled'` |

- 所有 id 用 `Utilities.getUuid().slice(0, 8)` 產生。
- 每次寫入後回傳 `{ ok: true, ...受影響的資料 }`，讓前端可以就地更新而不必重抓全部。

---

## 6. 畫面規格

採 hash routing（`#/`、`#/event/xxx`、`#/games`），單一 `index.html`，由 JS 切換 view。

### 6.1 首頁 `#/`

- 頂部：站名、「開新團」主要按鈕。
- **投票中的團**：卡片列表。每張卡顯示標題、發起者、候選時段（每個時段標示目前幾人可以）、地點、已投票人數 ／ 總成員數。
- **已定案的團**：卡片顯示確定的日期時間、地點、報名人數、指定遊戲（若有）。
- **已結束**：預設收合，點開才顯示。
- 空狀態：顯示「目前沒有進行中的團」＋開團按鈕，不要只留一片空白。

### 6.2 開新團 `#/new`

表單欄位，依序：

1. **團名**（必填，例「週末來玩重策」）
2. **發起人**（從 `person` 成員下拉選）
3. **候選時段**（2–4 組，每組：日期選擇器 ＋ 下午／晚上二選一）。預設給 2 組，可按「再加一個時段」，上限 4 組。
4. **地點**（下拉選 `venues`，或選「其他」後出現自由輸入框；可留空）
5. **想玩的遊戲**（選填，但**放在顯眼位置並加上鼓勵填寫的提示文字**，例：「有指定想玩的嗎？寫出來比較叫得動人」）。以搜尋框從 `games.json` ＋ Sheets `collections` 挑選，可多選。
6. **備註**（選填）

送出時要求輸入共用密碼（若 localStorage 已有則不再問）。

### 6.3 團頁 `#/event/<id>`

由上而下：

1. **標題列**：團名、狀態徽章（投票中／已定案／已取消）、發起者。
2. **時段投票表**：橫軸為候選時段，縱軸為成員。先讓使用者從 `person` 成員清單點自己的名字（選過後存 localStorage，下次自動帶入，但要有「不是我？」的切換連結）。點名後每個時段可切換「可以／不行」。即時顯示每個時段的「可以」人數。
3. **定案區**（`status=open` 時顯示）：選一個時段 ＋ 確認地點，按「定案」。按鈕旁註明「定案後會把投可以的人自動加入報名名單」。
4. **報名名單**（`status=confirmed` 時顯示）：列出已報名者，每人可退出；未報名者可加入。
5. **可玩清單**：依 §3.3 計算。
   - 上方一排篩選：**人數**（預設帶入目前報名人數）、**時長上限**（下拉：≤30 / ≤60 / ≤90 / ≤120 / 不限）、關鍵字。
   - 每款一張小卡：縮圖、中文名、人數、時長、複雜度、來源標籤（`場地現有` / `阿明 可帶來`）、BGG 連結。
   - 若團有指定遊戲，置頂並標示「已指定」。
   - 清單為空時，說明原因（「還沒有人報名，也還沒選地點」），不要顯示空白。

### 6.4 遊戲庫 `#/games`

- 全部遊戲（`games.json` ＋ Sheets `collections` 合併）。
- 篩選列：關鍵字（中英文名皆可搜）、持有者、分類代碼、人數、時長、複雜度區間。
- 點一款展開詳情：縮圖、中英文名、人數、時長、複雜度、分類、**這款在哪些地點／誰手上**（這就是需求的第三點：遊戲反查）、BGG 連結、代理商連結與售價（若有）。
- **反查頁不提供開團功能。** 這是刻意的決定，不要順手加上去。
- 擴充品項不獨立列出，附在母款詳情裡，灰字縮排顯示。

### 6.5 全域行為

- **已結束判定**：`status='confirmed'` 且確定時段的日期早於今天 → 視為已結束，移到首頁的收合區。資料**保留不刪**。
- **密碼**：讀取不需要密碼。任何寫入動作前檢查 localStorage 的 `write_secret`，沒有就跳出輸入框。伺服器回 `BAD_SECRET` 時清掉 localStorage 並重新詢問。
- **錯誤處理**：任何 API 失敗都要在畫面上顯示可讀的中文訊息並提供「重試」，不要只 `console.error`。
- **載入狀態**：`bootstrap` 期間顯示骨架或載入字樣（Apps Script 有冷啟動，首次可能 1–3 秒）。

---

## 7. BGG 抓取腳本

`scripts/build-games.mjs` — **只在本機執行**，不進部署，不加入任何執行階段依賴。

輸入：
- `自然樹的桌遊清單.xlsx`（主檔：中文名、英文名、BGG 連結、代理商連結、售價、代理商、購買年份、備註；每個分類一個分頁）
- `桌遊資料庫.xlsx`（英文名、BGG 連結、代理商商品連結、官方售價、代理商）

流程：

1. 讀取 xlsx，從 BGG 連結解析 `bgg_id`（URL 形如 `https://boardgamegeek.com/boardgame/224517/brass-birmingham`）。分頁名對應到分類代碼。
2. 呼叫 `https://boardgamegeek.com/xmlapi2/thing?id=<逗號分隔的 ids>&stats=1`，**每次最多 20 個 id**，每批之間 sleep 2 秒。BGG 有速率限制，太快會被擋。
3. 若回應為 HTTP 202（排隊中），等 5 秒重試，最多 5 次。
4. 從 XML 取出：`name[@type="primary"]`、`thumbnail`、`minplayers`、`maxplayers`、`playingtime`、`minplaytime`、`maxplaytime`、`statistics/ratings/averageweight`、`yearpublished`、`@type`（`boardgameexpansion` 即為擴充）。
5. 輸出 `data/games.json` 與 `data/collections.json`。
6. **額外**：接受一個可選參數，讀取 Sheets `collections` 分頁匯出的 CSV，把其中 `games.json` 沒有的 `bgg_id` 一併抓進來，讓朋友的收藏也有完整後設資料。

腳本本身可以用 npm 套件（如 `xlsx`），但這些依賴放在 `scripts/` 底下獨立的 `package.json`，且 `scripts/` 必須被 `.vercelignore` 排除。

---

## 8. 檔案結構

```
/
├── index.html
├── robots.txt              # Disallow: /
├── .vercelignore           # 排除 scripts/ 與 apps-script/
├── css/
│   └── style.css
├── js/
│   ├── app.js              # 啟動、hash router
│   ├── api.js              # Apps Script 讀寫封裝
│   ├── store.js            # bootstrap 資料與合併邏輯
│   ├── rules.js            # 可玩清單計算、篩選、已結束判定
│   └── views/
│       ├── home.js
│       ├── newEvent.js
│       ├── event.js
│       └── games.js
├── data/
│   ├── games.json
│   └── collections.json
├── apps-script/
│   └── Code.gs             # 貼進 Apps Script 編輯器用，不部署到 Vercel
└── scripts/
    ├── package.json
    └── build-games.mjs
```

`index.html` 的 `<head>` 需含：

```html
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="robots" content="noindex, nofollow">
```

---

## 9. 驗收標準

依序驗證，每一項都要能實際操作通過：

1. 首頁在沒有任何團的情況下正確顯示空狀態。
2. 能建立一個含 3 個候選時段的團，重新整理後仍在。
3. 兩個不同成員分別投票，票數即時正確累加；同一人改票是更新而非新增列。
4. 定案後，對該時段投「可以」的人自動出現在報名名單。
5. 可玩清單正確等於「報名者收藏 ∪ 場地持有者收藏」，且來源標籤正確。
6. 新增一位報名者後，可玩清單立刻多出他的收藏。
7. 人數篩選預設帶入報名人數，且能正確排除人數不符的遊戲。
8. 在 Sheets 手動新增一筆 `collections` 且該 `bgg_id` 不在 `games.json` 中 → 該款仍顯示（用 `name_zh`），標示缺少資料，且不出現在人數／時長篩選結果中。
9. 遊戲庫搜尋任一款，詳情正確列出「在哪些地點／誰手上」。
10. 密碼錯誤時寫入被拒，畫面顯示可讀的中文錯誤。
11. 手機寬度 375px 下所有畫面可用，無橫向捲動。
12. 確定時段日期設為昨天 → 該團自動移入首頁的已結束收合區。

---

## 10. 明確不要做的事

以下都是已經討論並刻意排除的，**不要好心加上去**：

- 登入、帳號、OAuth、個人化
- 任何自動通知（LINE bot、Email、推播）
- when2meet 式的格狀時間表
- 投票的第三態（「勉強可以」）
- 湊滿人數自動成團
- 從遊戲反查頁直接開團
- 桌遊店的營業時間、低消、地圖、交通資訊
- 執行階段呼叫 BGG API
- 前端框架、打包工具、CSS 框架、TypeScript
- 把遊戲縮圖下載進 repo（直接用 BGG 的圖片網址）
- 為擴充品項建立獨立分類

---

## 11. 已知風險（實作時留意，不要試圖修掉）

- **密碼不是真的安全**：它寫在前端請求裡，任何人打開開發者工具就看得到。它的定位是「減速丘」，擋掉誤入者與爬蟲，不是防攻擊。不要為此加密或混淆，也不要改成更複雜的方案。
- **收藏資料躺在兩個地方**（repo JSON 與 Sheets），合併時欄位必須一致，否則會出現只有一半資料的遊戲卡片。§9 第 8 項就是在驗這件事。
- **Apps Script 有每日呼叫配額與冷啟動延遲**。`bootstrap` 一次抓完所有資料就是為了壓低呼叫次數，不要拆成多個 API 各自呼叫。
- **通知靠人工**：開團的人要自己把網址貼到 LINE 群組。若他忘了貼，這團就沒人知道。這是已知且接受的取捨。
